<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/tailwindpresets.css" />
    <title><%= pageTitle %></title>
  </head>
  <body class="flex min-h-[100dvh] flex-col">
    <%- include('header') -%>
    <main
      class="mx-6 mb-14 flex flex-1 flex-col items-center justify-center px-4 py-6"
    >
      <div
        class="min-w-[300px] rounded-xl bg-slate-200 px-10 py-12 pb-5 shadow-md"
      >
        <h1
          class="mx-3 border-b border-slate-400 pb-2 text-center text-2xl font-semibold"
        >
          <%= pageTitle %>
        </h1>
        <div class="py-3">
          <h2>
            Are you <strong>sure</strong> you want to delete this blog entry and
            <strong>all</strong> associated comments?
          </h2>
        </div>
        <div class="flex items-center justify-center gap-5">
          <a href="/">
            <button
              class="mt-3 rounded-md border-[1px] border-solid border-gray-700 p-[10px] text-gray-700 transition-[color] duration-[0.1s] ease-linear hover:border-black hover:text-black"
            >
              Cancel
            </button>
          </a>
          <form method="POST">
            <button
              class="mt-3 rounded-md border-[1px] border-solid border-gray-900 p-[10px] text-gray-900 shadow-md transition-[color] duration-[0.1s] ease-linear hover:border-black hover:text-black"
              type="submit"
            >
              Delete
            </button>
          </form>
        </div>
      </div>
    </main>
  </body>
</html>
